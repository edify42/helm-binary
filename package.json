{
  "name": "helm-binary",
  "version": "3.2.4-release",
  "description": "install helm binary to you node_modules",
  "bin": {
    "helm": "bin/helm"
  },
  "scripts": {
    "clean": "rm -rf bin && mkdir bin",
    "destroy": "yarn run clean && rm -rf node_modules",
    "lint-markdown": "yarn run markdownlint README.md",
    "make": ".ci/get-helm.sh latest",
    "publish": "yarn publish",
    "postinstall": "rm bin/helm && echo 'creating a hard link for helm' && ln node_modules/helm-binary-linux/bin/helm bin/helm || ln node_modules/helm-binary-darwin/bin/helm bin/helm"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/edify42/helm-binary"
  },
  "author": "ekim8015@gmail.com",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "markdownlint-cli": "^0.23.2"
  },
  "optionalDependencies": {
    "helm-binary-darwin": "^3.2.4",
    "helm-binary-linux": "^3.2.4"
  }
}
